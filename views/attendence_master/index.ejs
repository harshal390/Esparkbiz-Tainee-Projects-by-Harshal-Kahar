<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Filter with Pagination</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <%- include('partials/filter'); %>
    <div class="_table w-full flex items-center justify-center">
        <table class="table-auto inline">
            <thead class="w-full">
                <tr class="flex">
                    <% for(let i=0; i<table_title.length; i++) {%>
                        <th class="border capitalize text-center py-2 px-5 flex items-center !w-[204px] gap-5">
                            <%= table_title[i] %>
                                <span class="flex flex-col w-fit">
                                    <span class="cursor-pointer">⬆</span>
                                    <span class="cursor-pointer">⬇</span>
                                </span>
                        </th>
                        <% }; %>
                </tr>
            </thead>
            <tbody>
                <% for(let i=0; i<students.length; i++) {%>
                    <tr>
                        <% for(let j=0; j<table_keys.length; j++) {%>
                            <td class="border text-center py-2 px-5  inline-block w-[200px]">
                                <%= students[i][table_keys[j]] %>
                            </td>
                            <% }; %>
                    </tr>
                    <% }; %>
            </tbody>
        </table>
    </div>
    <div class="h-[10rem] flex items-center justify-center">
        <%- include('partials/pagination'); %>
    </div>

</body>

</html>